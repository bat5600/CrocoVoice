# Assets Directory

Placez votre icône de tray ici avec le nom `tray-icon.png`.

L'icône doit être :
- Format : PNG
- Taille recommandée : 16x16 ou 32x32 pixels
- Fond transparent (optionnel mais recommandé)

Si aucune icône n'est fournie, l'application utilisera une icône par défaut.
